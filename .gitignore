# v0 runtime files (should only exist in preview, not in git)
__v0_runtime_loader.js
__v0_devtools.tsx
__v0_jsx-dev-runtime.ts

# Common ignores
node_modules/
.next/
.env*.local
.DS_Store

# TypeScript build cache (gerado pelo build, não versionar)
*.tsbuildinfo

# Vídeos grandes (GitHub limita a 100 MB)
public/Site Larafy animations.mov

# Repositório aninhado (evitar adicionar por engano)
Site-larafy/